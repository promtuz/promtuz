<?xml version="1.0" encoding="utf-8"?>
<resources>
    <paths>
        <cache-path
            name="shared_images"
            path="images/" />
        <cache-path
            name="update_apks"
            path="updates/" />
        <cache-path
            name="shared_logs"
            path="logs/" />
        <!-- Downloaded attachments live in app-private storage; expose them so the
             file card's open action can hand them to a viewer. -->
        <files-path
            name="files"
            path="." />
        <external-files-path
            name="ext_files"
            path="." />
    </paths>
</resources>
