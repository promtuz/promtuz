<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Calls: a foreground service with the microphone type, and a
         full-screen intent so an incoming call comes up over the lock screen.
         No Telecom (ConnectionService), on purpose — see CALLS.md. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <application
        android:name=".Promtuz"
        android:allowBackup="true"
        android:allowClearUserData="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:manageSpaceActivity=".ui.activities.ManageSpace"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Promtuz">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.promtuz.chat.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".LauncherActivity"
            android:configChanges="orientation|screenSize|screenLayout"
            android:exported="true"
            android:theme="@style/Theme.App.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Remote pairing: https://promtuz.dev/pair#<code> App Link. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="promtuz.dev"
                    android:pathPrefix="/pair"
                    android:scheme="https" />
            </intent-filter>

            <!-- Fallback for web pages / in-app browsers: promtuz://pair#<code>. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="pair"
                    android:scheme="promtuz" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.ManageSpace"
            android:exported="false"
            android:label="@string/title_activity_manage_space" />

        <!-- Message wakes and app-release hints (see push/PushService). -->
        <service
            android:name="com.promtuz.core.push.PushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Inline notification actions (reply / mark-read); fired only by our own PendingIntents. -->
        <receiver
            android:name="com.promtuz.core.push.ReplyReceiver"
            android:exported="false" />
        <receiver
            android:name="com.promtuz.core.push.MarkReadReceiver"
            android:exported="false" />
        <service
            android:name="com.promtuz.core.AppCloseService"
            android:exported="false"
            android:stopWithTask="false" />

        <!-- Call: holds the microphone, the ongoing notification and the
             network watch for the length of a call. -->
        <service
            android:name="com.promtuz.core.call.CallService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- The full-screen call, shown over the lock screen. Its own task so
             ending a call never lands back on a stale call screen. -->
        <activity
            android:name="com.promtuz.core.call.CallActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:taskAffinity="com.promtuz.chat.call"
            android:theme="@style/Theme.Promtuz" />

        <!-- Answer / hang-up buttons on the call notification. -->
        <receiver
            android:name="com.promtuz.core.call.CallActionReceiver"
            android:exported="false" />
    </application>

</manifest>
